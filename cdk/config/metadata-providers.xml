<?xml version="1.0" encoding="UTF-8"?>
<MetadataProvider id="ShibbolethMetadata"
    xsi:type="ChainingMetadataProvider"
    xmlns="urn:mace:shibboleth:2.0:metadata"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="urn:mace:shibboleth:2.0:metadata
        http://shibboleth.net/schema/idp/shibboleth-metadata.xsd">

    <!--
        Cognito Service Provider — static metadata.
        Cognito does not expose a metadata XML endpoint so we provide SP
        details directly. Placeholders are substituted by startup.sh.
    -->
    <MetadataProvider id="CognitoSP"
        xsi:type="FilesystemMetadataProvider"
        metadataFile="%{idp.home}/conf/cognito-sp-metadata.xml">
    </MetadataProvider>

</MetadataProvider>
